<h1>イラスト一覧</h1>

<% @arts.each do |art| %>

<div>
    <!--画像-->
    <p><%= image_tag art.image, size: "200x200" %></p>
    <!--タイトル-->
    <p><%= link_to art.title, art_path(art.id) %></p>
    <!--コメント数-->
    <p><%= link_to "#{art.art_comments.count}コメント", art_path(art.id) %></p>
</div>
<% end %>